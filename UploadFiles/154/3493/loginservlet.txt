 StringBuffer jb = new StringBuffer();
		  String line = null;
		  BufferedReader reader = request.getReader();
		    while ((line = reader.readLine()) != null)
		      jb.append(line);
		    org.json.JSONObject jsonObject =  HTTP.toJSONObject(jb.toString());
		    String data=(String) jsonObject.get("Method");
		    
		
		    JsonObject person = new JsonObject();
		    person.addProperty("name","phani");
		    person.addProperty("pass","kumar");
		  
		    System.out.println(person.get("name"));
		    
		  /*  Iterator it = jsonObject.keys(); //gets all the keys

		    while(it.hasNext())
		    {
		        String key = (String) it.next(); // get key
		        Object o = jsonObject.get(key); // get value
		        System.out.println(key + " : " +  o); // print the key and value
		    }*/	
		        response.setContentType("text/html");  
		    
		        HttpSession session=request.getSession();
		        session.setAttribute("UserName",person.get("name").toString());
		        
		        SessionData sessinData=new SessionData();
		        sessinData.setSessionUserName((String) request.getSession(false).getAttribute("UserName"));
		    RequestDispatcher requestDispatcher=   request.getRequestDispatcher("/Home.html");
		    requestDispatcher.forward(request, response);